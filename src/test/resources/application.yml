spring:
  jpa:
    database: h2
    database-platform: org.hibernate.dialect.H2Dialect
    generate-ddl: false
    show-sql: false
  h2:
    console:
      enabled: true
      path: /h2-console
  output:
    ansi:
      enabled: always
server:
  address: 0.0.0.0
  port: 6180